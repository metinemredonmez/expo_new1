// redux/reducers/authReducer.js
const initialState = {
    // define your initial state for auth here
};

const authReducer = (state = initialState, action) => {
    // implement your auth reducer logic here
    return state;
};

export default authReducer;
