// redux/reducers/categoryReducer.js
const initialState = {
    // define your initial state for category here
};

const categoryReducer = (state = initialState, action) => {
    // implement your category reducer logic here
    return state;
};

export default categoryReducer;
