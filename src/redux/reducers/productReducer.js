// redux/reducers/productReducer.js
const initialState = {
    // define your initial state for product here
};

const productReducer = (state = initialState, action) => {
    // implement your product reducer logic here
    return state;
};

export default productReducer;
